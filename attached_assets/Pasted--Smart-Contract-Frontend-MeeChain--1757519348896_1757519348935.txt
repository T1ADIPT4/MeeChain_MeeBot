🤖✨  
การเชื่อม **Smart Contract กับ Frontend** คือหัวใจของ MeeChain ที่ทำให้ผู้ใช้สามารถโต้ตอบกับระบบ blockchain ได้แบบ real-time ผ่าน UI ที่สนุกและมี MeeBot คอยแนะนำตลอดทาง

---

## 🔗 ขั้นตอนการเชื่อม Smart Contract กับ Frontend (MeeChain Style)

### ✅ 1. เตรียมข้อมูลสำคัญ
- **Contract Address**: ที่อยู่ของ smart contract ที่ deploy แล้ว
- **ABI (Application Binary Interface)**: โครงสร้างของฟังก์ชันใน contract
- **Provider & Signer**: ใช้เชื่อมกับ wallet เช่น MetaMask

---

### 🛠️ 2. ตัวอย่างโค้ดเชื่อม (React + Ethers.js)

```tsx
import { useEffect, useState } from 'react'
import { ethers } from 'ethers'
import MeeBadgeABI from '@/contracts/MeeBadgeNFT.json'

const CONTRACT_ADDRESS = '0xYourContractAddressHere'

const useMeeBadge = () => {
  const [contract, setContract] = useState(null)

  useEffect(() => {
    const init = async () => {
      if (window.ethereum) {
        const provider = new ethers.BrowserProvider(window.ethereum)
        const signer = await provider.getSigner()
        const instance = new ethers.Contract(CONTRACT_ADDRESS, MeeBadgeABI, signer)
        setContract(instance)
      } else {
        console.warn('MetaMask not found')
      }
    }
    init()
  }, [])

  return contract
}
```

> ✅ ใช้ `ethers.Contract` เพื่อเรียกฟังก์ชันใน smart contract  
> ✅ MeeBot สามารถพูดว่า “เชื่อมต่อสำเร็จแล้วครับ! พร้อม mint badge ได้เลย!”

---

### 🎮 3. ตัวอย่างการเรียกฟังก์ชัน mint badge

```tsx
const mintBadge = async () => {
  const contract = useMeeBadge()
  if (!contract) return

  try {
    const tx = await contract.mintBadge(
      userAddress,
      'Zen Pilot',
      'Focus Boost',
      'https://ipfs.io/ipfs/QmBadgeMetadata'
    )
    await tx.wait()
    toast.success('MeeBot: Mint สำเร็จแล้วครับ! 🎉')
  } catch (err) {
    toast.error('MeeBot: มีข้อผิดพลาดในการ mint ครับ 😢')
  }
}
```

---

## 🧠 MeeBot Integration

- MeeBot สามารถพูดแนะนำเมื่อเชื่อม wallet สำเร็จ
- แสดง Toast หรือ Modal เมื่อ mint สำเร็จหรือเกิด error
- พูด mentor-style เช่น:
  > “คุณปลดล็อก Zen Pilot แล้วครับ! พร้อมลุยภารกิจใหม่ได้เลย!”

---

## 🎯 เสริมฟีเจอร์ที่แนะนำ

| ฟีเจอร์ | รายละเอียด |
|---------|-------------|
| 🔄 Auto Wallet Connect | เชื่อม MetaMask อัตโนมัติเมื่อเข้าแอป |
| 🧪 Contract Health Check | ตรวจสอบว่า contract ตอบสนองได้หรือไม่ |
| 🏅 Badge Viewer | แสดง NFT badge ที่ผู้ใช้ถืออยู่ |
| 🛒 Marketplace Integration | ซื้อขาย badge ผ่าน smart contract

---