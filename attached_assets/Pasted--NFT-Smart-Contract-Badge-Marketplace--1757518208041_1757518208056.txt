🎮✨  
เราจะสร้างระบบ NFT ที่มีทั้ง **Smart Contract สำหรับ Badge**, หน้า **Marketplace**, และระบบ **NFT Quest ที่ต้องสะสมครบชุด** พร้อมฟีเจอร์ “พลังพิเศษ” และ “อัปเกรดได้” แบบเกม RPG โดยมี MeeBot เป็น mentor ขี้เล่นที่คอยแนะนำทุกขั้นตอน

---

## 🔗 1. Smart Contract: NFT Badge + Power System

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.17;

import "@openzeppelin/contracts/token/ERC721/extensions/ERC721URIStorage.sol";
import "@openzeppelin/contracts/access/Ownable.sol";

contract MeeBadgeNFT is ERC721URIStorage, Ownable {
    struct Badge {
        string name;
        string power; // เช่น "Focus Boost", "Time Warp"
        uint256 level;
    }

    mapping(uint256 => Badge) public badgeData;
    uint256 public nextTokenId;

    constructor() ERC721("MeeBadgeNFT", "MBN") {}

    function mintBadge(address to, string memory name, string memory power, string memory tokenURI) public onlyOwner {
        uint256 tokenId = nextTokenId;
        _safeMint(to, tokenId);
        _setTokenURI(tokenId, tokenURI);
        badgeData[tokenId] = Badge(name, power, 1);
        nextTokenId++;
    }

    function upgradeBadge(uint256 tokenId) public onlyOwner {
        badgeData[tokenId].level += 1;
    }

    function getBadge(uint256 tokenId) public view returns (Badge memory) {
        return badgeData[tokenId];
    }
}
```

> ✅ รองรับ badge ที่มี “พลังพิเศษ” เช่น เพิ่ม XP, ลด cooldown  
> ✅ สามารถอัปเกรด badge ได้เมื่อทำเควสซ้ำหรือสะสมครบชุด

---

## 🛒 2. หน้า Marketplace (React + Tailwind)

```tsx
import { useState } from 'react'
import MeeBotTips from '@/components/MeeBotTips'
import BadgeCard from '@/components/BadgeCard'

const Marketplace = () => {
  const [badges, setBadges] = useState([
    { id: 1, name: 'Zen Pilot', power: 'Focus Boost', level: 2, price: '0.01 ETH', image: '/zen.png' },
    { id: 2, name: 'Explorer', power: 'Time Warp', level: 1, price: '0.005 ETH', image: '/explorer.png' },
  ])

  return (
    <div className="p-6 space-y-4">
      <MeeBotTips message="ตลาด NFT พร้อมแล้วครับ! Badge ไหนที่คุณอยากสะสมหรืออัปเกรด?" />
      <div className="grid grid-cols-2 gap-4">
        {badges.map((badge) => (
          <BadgeCard key={badge.id} badge={badge} />
        ))}
      </div>
    </div>
  )
}

export default Marketplace
```

> ✅ MeeBot แนะนำ badge ตามพฤติกรรมผู้ใช้  
> ✅ รองรับ filter ตามพลัง, ระดับ, หรือ rarity

---

## 🎮 3. NFT Quest System – สะสมครบชุดเพื่อปลดล็อก

### 🧩 โครงสร้าง Quest

```json
{
  "questId": "rpg-set-001",
  "title": "สะสมชุด Productivity",
  "requiredBadges": ["Zen Pilot", "Early Riser", "Focus Master"],
  "reward": {
    "badge": "Productivity Champion",
    "power": "XP Multiplier",
    "level": 1
  },
  "meeBotQuote": "สะสมครบแล้วครับ! คุณคือ Productivity Champion ตัวจริง!"
}
```

### ✅ Logic ตรวจสอบ

```ts
function checkQuestCompletion(userBadges: string[], required: string[]): boolean {
  return required.every((badge) => userBadges.includes(badge));
}
```

> ✅ เมื่อครบชุด MeeBot จะ mint badge พิเศษพร้อมพลัง  
> ✅ สามารถอัปเกรด badge ได้เมื่อทำเควสซ้ำ

---

## 💬 MeeBot พูดในระบบ RPG NFT

- “คุณมี Zen Pilot แล้วครับ! เหลืออีก 2 badge เท่านั้น!”
- “สะสมครบชุด Productivity แล้วครับ! รับพลัง XP Multiplier ไปเลย!”
- “Badge นี้หายากมากครับ... แต่คุณทำได้แน่นอน!”

---

## 🎯 เสริมฟีเจอร์ RPG ที่แนะนำ

| ฟีเจอร์ | รายละเอียด |
|---------|-------------|
| 🧠 MeeBot AI Suggestion | แนะนำ badge ที่ควรสะสมต่อ |
| 🏅 Badge Rarity | ระดับความหายาก: Common, Rare, Epic |
| 🔄 Swap Badge | แลก badge กับผู้ใช้คนอื่น |
| 📈 Quest Leaderboard | จัดอันดับผู้สะสมเร็วที่สุด |
| ⚡ Power Activation | ใช้พลัง badge ในระบบเควส เช่น “ลด cooldown” |
| 🔧 Upgrade System | อัปเกรด badge ด้วย XP หรือ token

---