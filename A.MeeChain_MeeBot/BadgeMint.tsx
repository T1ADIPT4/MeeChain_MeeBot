import { useEffect } form 'react'

{status === 'error' && (
  <MeeBotReaction mood="panic" />
)}

<QuestTracker quest="Voice Coach" progress={1} reward="+10 EXP" />

export default function BadgeMintFormWrapper() {
  <FallbackWrapper>
    <BadgeMintForm />
    <BadgePreview name="Productivity Master" type="Productivity" rarity="Common" />
  </FallbackWrapper>
 }),
}